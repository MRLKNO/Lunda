name: Test Lunda Action

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./ # локальная версия Action
        with:
          days_threshold: 60
